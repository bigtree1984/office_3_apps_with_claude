# POTX / DOTX / グラフの生成に必要なもの（いずれも標準ライブラリで足りない部分だけ）
fonttools>=4.50   # フォント埋め込み（EOT / ODTTF のヘッダー作成）
pillow>=10.0      # 画像サイズの取得・圧縮
ezdxf>=1.1        # ロゴ：Fusion の DXF を読む（dxf_to_svg.py を使うときだけ）
shapely>=2.0      # ロゴ：閉じた輪郭を塗りの図形にまとめる（同上）
pymupdf>=1.24     # 検証：PDF を画像にして目視確認する（任意）

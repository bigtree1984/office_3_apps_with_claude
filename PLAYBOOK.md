# PLAYBOOK — 自分専用の Office デザインシステムを AI と作る手順

このファイルは **AI エージェント（Claude Code など）に読ませる前提**で書いています。
人は「何を決めるか」に集中し、手順と XML は AI に任せます。

実際に1つ作り切ったときの道順をそのまま書き起こしたものです。所要は、対話しながらで数日（作業時間ではなく、判断の回数がボトルネック）。

---

## 0. 前提：人が渡す3つ

| 渡すもの | 無いとどうなるか |
|---|---|
| **仮説**（「ZIP の中の XML を書けば、テーマもガイドも設定できるはず」） | AI は「そのライブラリでは無理です」で止まる |
| **見本の現物**（正解の POTX が1つあれば最良。無ければ AI が機能の地図を描く） | 推測で書いて細部を外す |
| **検証の手足**（実際に Office で開いて見る人・環境） | 出せない、または確かめずに出す |

逆に**手順の指示は要らない**。手順は AI が組む。人が握るのは、目的・現物・判定。

---

## 1. SSOT を先に決める（最重要）

色や文字を決める前に、**どの情報の正をどこに置くか**を決める。ここを飛ばすと後で全部やり直しになる。

AI と共同でデザインを管理すると、次の4つが**別々の場所に分かれうる**。

- 見た目を確かめる場所
- SSOT（正）を残す場所
- AI がアクセスしやすい場所
- スクリプトから参照しやすい場所

人だけで作るなら前の2つは同じデザインツールで済む。AI とスクリプトが加わると分かれる。

**この一式での結論**（`bigtree/design/SSOT_MAP.md`）：

| 層 | 正 | 理由 |
|---|---|---|
| 共通トークン（色・フォント） | **Figma の変数** | 目で決める値 |
| PowerPoint のレイアウト・図解 | **Figma** | 位置と見た目を目で決める |
| グラフの作法 | **設定ファイル（JSON）** | Figma にグラフ機能が無い。描くのは Office 自身 |
| Word のスタイル | **md のスタイル表** | 流れる文章の規則。Figma の「位置の決まった画面」と合わない |
| 表のスタイル | **POTX / DOTX の XML** | Figma のデザインファイルに表機能が無い |

→ Figma が正でない層では、**Figma は「Office が実際に描いた結果を貼る見本帳」**にする。

---

## 2. 人に聞くこと（AI へ：この順で質問する）

1. **何に使う資料か**（例：定型の運用報告 / 提案書）。種類によって必要な図解の数が変わる
2. **渡し方**：編集できる .pptx/.docx で渡すか、PDF か。フォントを埋め込むかが決まる
3. **色塗りポリシー**：「重要なもの何個までを有彩色にするか」→ **テーマ色の並び順**に落とす（§3）
4. **スライドの大きさ**：Google スライドと行き来するなら 10×5.625 インチ、PowerPoint 中心なら 13.333×7.5 インチ
5. **Word を使うか**：使うなら「改ページのない表示にするか」（Web レイアウト）
6. **グラフは数字に触らせるか**：触らせるならネイティブグラフ、見せるだけなら画像
7. **ロゴ・キービジュアルの有無**：無ければ枠だけ作って後で差し替える

---

## 3. 共通トークン：ポリシーを「並び順」に埋め込む

Office は**グラフの系列を accent1 → accent2 → … の順に自動で塗る**。図形の既定の塗りも accent1。
つまり**並び順がそのままポリシーになる**。

例（「有彩色は重要な1〜2個だけ。残りはグレー」というポリシーの場合）：

| 枠 | 割り当て |
|---|---|
| accent1 / accent2 | 有彩色2色（白黒でも区別できる組み合わせに） |
| accent3 / 4 / 5 | グレー3段（**明るさを等間隔**に。色相ではなく明るさで分ける） |
| accent6 | 強調色（**自動では使われない位置**に置く。指定したときだけ出る） |

→ 人が普通にグラフを挿入するだけで、ポリシーどおりに塗られる。AI を通さなくても守られる。

文字サイズは **本文 × 一定比率のべき乗**（例：1.25）で役割ごとに決める。
**決め方は「3案の試し書きを作って見比べてもらう」**のが速い。数値の議論より判断が早い。

---

## 4. 作る順番

```
① SSOT を決める              → SSOT_MAP.md
② 共通トークン                → Figma の変数 → tokens.json に書き出し
③ ロゴ・キービジュアル         → SVG / 画像（無ければ枠だけ）
④ スライドレイアウト          → Figma（1440×810px なら 2px = 1pt で扱いやすい）
⑤ Figma → POTX の変換         → figma_layouts.json → build_potx_figma.py
⑥ Word のスタイル表 → DOTX    → WORD_STYLE_SPEC.md → build_dotx.py
⑦ 図解（Organism）            → Figma で作図 → figma_organisms.json → build_organisms.py
⑧ グラフ・表                  → build_charts.py / 表スタイルは POTX 内
```

各段階で必ず **「Office で開いて確認」** を挟む。AI は PDF に書き出して自分で画像として確認できるが、
**人が普通に開いたときの見え方は人にしか分からない**（表示モード、ダイアログ、フォント環境）。

---

## 5. レイアウトは何種類作るか

「レイアウトに入れるもの」と「スライドごとに作るもの」の線引き：

- **レイアウトに入れる**：どのスライドでも同じ位置に来るもの／人がうっかり動かすと困るもの／PowerPoint の機能で ON・OFF したいもの
- **スライドごとに作る**：図解・グラフ・表・2段組み・目次など、中身で変わるもの

この一式では7種類にした。

| # | レイアウト | 用途 |
|---|---|---|
| 01 | 表紙 | 精緻に作り込む（唯一の作り込み対象） |
| 02 / 03 | 扉（Light / Dark） | 章扉、Thank you |
| 04 / 05 | 構造（Light / Dark） | 目次、写真ギャラリー（装飾線を置かない） |
| 06 / 07 | 本文（リードあり / なし） | 本文、資料・表 |

**装飾をレイアウトに固定すると、スライド側で消せない。** 目次やギャラリーで邪魔になるなら、用途ごとにレイアウトを分ける。

---

## 6. 必ず踏む罠（先に読むと時間が浮く）

> **指定しない＝Office の既定が出る。**

| 罠 | 対処 |
|---|---|
| 表の外枠・縦罫が勝手に出る | 「線なし」も明示する（`<w:val="none">` / `<a:ln><a:noFill/></a:ln>`） |
| グラフの3番目の系列に強調色が付く | 強調色を accent6 に置く（§3） |
| 文字が下にずれる／行間が広い | 行間は pt 指定、テキストボックスは縦中央、内側余白 0（`notes/TEXT_LAYOUT_NOTES.md`） |
| Word で挿絵が消える | 「図」スタイルだけ行間を auto に（固定値は行内画像を切り詰める） |
| Word の行間が倍に開く | ドキュメントグリッドを無効化（日本語 Word の既定は行を格子に吸着させる） |
| 「コンテンツに問題が見つかりました」 | マスターのプレースホルダーに種類（`type="body"` など）を書く。**修復は押さない**（原因が消える） |
| フォント埋め込みが保存で消えた | **自動操作（AppleScript）での保存では消える**。人が画面から保存すれば残る |
| 日本語フォントが「使用文字だけ」に削られる | Word の仕様。提出直前に AI が全文字を入れ直す運用にする |

詳細は `notes/`。

---

## 7. 確認の進め方（AI へ）

- **PDF に書き出して自分で見る**：Office に「名前を付けて保存」で PDF を作らせ、画像にして確認する。
  埋め込みフォントの確認にもなる（PDF に載っているフォント名を見る）。
- **往復テスト**：人が開いて保存し直したファイルを読み、書いた設定が残っているかを確かめる。
  スタイルの内部 ID は書き換わるが**名前は残る**ので、**名前で引く**。
- **失敗したら、まず「前は動いたファイル」で試す**。アプリ側が固まっているのか、ファイルが壊れているのかを一手で切り分けられる。
- **人が開いているファイルを閉じない**（未保存の作業を失う）。

---

## 8. 公開・共有するときの分け方

| 領域 | 中身 | 公開 |
|---|---|---|
| 研究・試作 | 検証ログ、調査メモ、比較用ファイル | しない（記事の材料にする） |
| ブランドの完成品 | テンプレート、トークン、ロゴ | 見本として公開するかは判断 |
| ノウハウ | 手順書・スクリプト・値が空の雛形 | **公開する** |

スクリプトは**ブランドの値を外から受け取る**作りにしておく（このリポジトリでは `OFFICE3_BRAND`）。
そうしておけば、他の人は自分のトークンとロゴを用意するだけで同じものを作れる。

# PLAYBOOK — 自分の POTX / DOTX を AI と作り、資料作成に使う

このファイルは **AI エージェント（Claude Code など）に読ませる前提**で書いています。
人は「何を決めるか」に集中し、XML と手順はエージェントに任せます。

実際に1つ作り切って、実データのレポートまで出した道順をそのまま書き起こしたものです。

**この一式でできること**：自分の配色・文字・レイアウトを入れた POTX（PowerPoint テンプレート）と
DOTX（Word テンプレート）を作り、そこから**エージェントに資料そのものを組ませる**。
グラフは画像ではなく**ネイティブグラフ**（相手が数字を編集できる）。
Excel・PowerPoint・Word の3つで、同じ配色・同じ文字サイズが揃います。

---

## エージェントへ：最初に読む順番

人から「このリポジトリを使って作って」と言われたら、この順で読むと最短で把握できます。

1. **この PLAYBOOK**（全体の流れと判断ポイント）
2. `bigtree/design/SSOT_MAP.md` — **どの情報の正をどこに置くか**。ここを外すと後で全部やり直しになる
3. `bigtree/design/tokens.json` — 色と文字サイズの正。**ここを差し替えれば全出力が変わる**
4. `notes/` — 実際に踏んだ罠（読むと数時間浮きます）
   `OOXML_BASICS.md` → `TEXT_LAYOUT_NOTES.md` → `TABLE_STYLE_NOTES.md` → `FONT_EMBEDDING.md`
5. `bigtree/design/CONTENT_RULES.md` と `CHART_RULES.md` — **資料を書くときの作法**。文章を書く前に読む
6. スクリプトは `scripts/build_potx_figma.py` から。`build_potx.py`（テーマ・フォント）→ `build_charts.py` の順

### 人が渡す3つ（これが無いと進まない）

| 渡すもの | 無いとどうなるか |
|---|---|
| **仮説**（「ZIP の中の XML を書けば、テーマもガイドも設定できるはず」） | エージェントは「そのライブラリでは無理です」で止まる |
| **見本の現物**（正解の POTX が1つあれば最良） | 推測で書いて細部を外す |
| **検証の手足**（実際に Office で開いて見る人・環境） | 出せない、または確かめずに出す |

逆に**手順の指示は要りません**。人が握るのは、目的・現物・判定です。

---

## 1. まず動かす（15分）

```bash
# 履歴は要らないので --depth 1 が速い（過去にフォント埋め込み版を含む時期があり、full clone は重い）
git clone --depth 1 https://github.com/bigtree1984/office_3_apps_with_claude
cd office_3_apps_with_claude
python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
```

Noto Sans JP の**静的フォント**（Regular / Bold）を `~/Library/Fonts` に置きます。
可変フォント（VariableFont）は埋め込みに使えません。別の場所に置くなら `OFFICE3_FONT_DIR` で指定します。

```bash
.venv/bin/python scripts/build_potx_figma.py   # POTX ＋ サンプル PPTX
.venv/bin/python scripts/build_dotx.py         # DOTX ＋ サンプル DOCX
.venv/bin/python scripts/build_charts.py       # Excel ＋ グラフ入りの PPTX / DOCX
```

`build/` の中身を、実際に PowerPoint と Word で開いてください。**ここで一度開くことが大事**です。
エージェントは PDF に書き出して自分で確認できますが、
**人が普通に開いたときの見え方は人にしか分かりません**（表示モード、ダイアログ、フォント環境）。

## 2. 自分のブランドに置き換える

最短は **`tokens.json` だけ差し替える**ことです。Figma が無くても始められます。

```bash
cp -r bigtree ~/mybrand                      # 一式をコピー
# ~/mybrand/design/tokens.json の colors と type_pptx / type_docx を書き換える
OFFICE3_BRAND=~/mybrand .venv/bin/python scripts/build_potx_figma.py
```

`tokens.json` が**色と文字サイズの唯一の正**です。ここを変えると、POTX のテーマ・マスター・
Word のスタイル・**グラフの中の文字サイズ**まで、まとめて変わります。

**ブランド名・著作権表示・ロゴ・出力ファイル名も同じファイルの `brand` ブロックから来ます。**

```json
"brand": {
  "name": "Bigtree Lab", "slug": "bigtree_lab",
  "url": "https://note.com/bigtree_lab", "copyright": "© 2026 Bigtree Lab",
  "logo": "assets/logo/260920_BT_logo.svg", "meta": "2026-09-18 ｜ Bigtree Lab ｜ だいき君"
}
```

`slug` が出力ファイル名（`<slug>.potx` / `<slug>_sample.pptx` …）、`copyright` がスライド下部と Word のフッター、
`meta` が表紙の日付・作成者、`logo` が表紙と構造レイアウトのロゴです。

**ロゴは普通の SVG で構いません**（複数パス・ベジェ曲線・多色・`<g transform>`・原点が 0,0 でない `viewBox`）。
多色のロゴは「1パス＝1図形」として重ねます。色が1色以下の SVG は、これまでどおりテーマ色で塗ります。
ゼロから始めるなら `templates/tokens.template.json` を写して埋めてください。

> **同じ値を2か所に置かない。** 一度 tokens.json と Figma 書き出しの両方に文字サイズを置いてしまい、
> トークンを変えても出力が変わらない状態になりました。**疑ったら、片方を極端な値にして反映を確かめる**。

### Figma を使わない場合

**`scripts/preview.py` が簡易プレビューです。** レイアウトと図解を1枚の HTML に描き、ブラウザで見られます。

```bash
.venv/bin/python scripts/preview.py --open
```

大事なのは「見る」ことより**「指させる」**ことです。要素にカーソルを乗せると**名前と座標**が出て、
クリックするとそのまま貼れる形でコピーされます（「`02_扉_Light` の `Title`（x,y,w,h = 72, 300, 1100, 80）を
少し下げて」のように、エージェントに正確に返せます）。
**文字のはみ出しと、地色に対して読めない文字（コントラスト 3.0 未満）も警告します。**
ただし**見え方の最終判断は人**です。暗地のブランドに変えた直後などは警告が大量に出ますが、
それは「ここを見ろ」という地図であって、全部直せという意味ではありません。

Office の実際の描画とは違うので、**行間の癖・表の罫線・グラフは PPTX にして確認**してください。
日常の検討はブラウザ、節目で PPTX という二段構えが速いです。

機能は意図的に小さく作ってあります。**足りないものは、あなたのエージェントに足させてください。**

### Figma を使う場合

レイアウトそのもの（表紙の作り、図解の形）を変えたいときは Figma を使います
（無料プランで足ります。ライブラリ公開や変数の複数モードを使わない限り）。
`bigtree/design/figma_layouts.json` が Figma からの書き出しで、位置と大きさは px。
**1440×810px で作ると 2px = 1pt** になり、換算が暗算でできます。
Figma を使わないなら、この JSON を直接手で書いても同じものが作れます。

## 3. 人が決めること（エージェントへ：この順で聞く）

1. **何に使う資料か**（定型の運用報告 / 提案書）。必要な図解の数が変わる
2. **渡し方**：編集できる .pptx/.docx か、PDF か → フォントを埋め込むかが決まる
3. **色塗りポリシー**：「有彩色は何個までにするか」→ **テーマ色の並び順**に落とす（§4）
4. **明地か暗地か**（白背景か、濃い背景か）。**影響が大きいのに見落としやすい判断**です。
   暗地にすると、`tokens.json` の色だけでは足りず、**レイアウト JSON の `bg` と要素の `color` を反転**する必要があります
   （`dk1` ↔ `lt1`）。先に決めておけば一度で済みます
5. **スライドの大きさ**：Google スライドと行き来するなら 10×5.625 インチ、PowerPoint 中心なら 13.333×7.5 インチ
6. **Word を使うか**：使うなら「改ページのない表示にするか」（Web レイアウト）
7. **グラフは数字に触らせるか**：触らせるならネイティブグラフ、見せるだけなら画像
8. **ロゴ・キービジュアルの有無**：無ければ枠だけ作って後で差し替える

文字サイズは **本文 × 一定比率のべき乗**（例：1.25）で決めます。
**決め方は「3案を試し書きして見比べてもらう」**のが速い。数値の議論より判断が早いためです。

## 4. 共通トークン：ポリシーを「並び順」に埋め込む

Office は**グラフの系列を accent1 → accent2 → … の順に自動で塗り**、図形の既定の塗りも accent1 です。
つまり**並び順がそのままポリシーになります**。

| 枠 | 割り当て（「有彩色は重要な1〜2個だけ」というポリシーの場合） |
|---|---|
| accent1 / accent2 | 有彩色2色（白黒でも区別できる組み合わせに） |
| accent3 / 4 / 5 | グレー3段（**明るさを等間隔**に。色相ではなく明るさで分ける） |
| accent6 | 強調色（**自動では使われない位置**に置く。指定したときだけ出る） |

→ 人が普通にグラフを挿入するだけで、ポリシーどおりに塗られます。**エージェントを通さなくても守られる**。

## 5. 作る順番

```
① SSOT を決める              → SSOT_MAP.md
② 共通トークン                → tokens.json（Figma を使うなら変数から書き出し）
③ ロゴ・キービジュアル         → SVG / 画像（無ければ枠だけ）
④ スライドレイアウト          → figma_layouts.json
⑤ POTX の生成                 → build_potx_figma.py
⑥ Word のスタイル表 → DOTX    → WORD_STYLE_SPEC.md → build_dotx.py
⑦ 図解（Organism）            → figma_organisms.json → build_organisms.py
⑧ グラフ・表                  → build_charts.py / 表スタイルは POTX 内
```

各段階で必ず **「Office で開いて確認」** を挟みます。

## 6. レイアウトは何種類作るか

- **レイアウトに入れる**：どのスライドでも同じ位置に来るもの／人がうっかり動かすと困るもの
- **スライドごとに作る**：図解・グラフ・表・目次など、中身で変わるもの

この一式では7種類（表紙／扉 Light・Dark／構造 Light・Dark／本文 リードあり・なし）。

**装飾をレイアウトに固定すると、スライド側で消せません。** 目次やギャラリーで邪魔になるなら、
用途ごとにレイアウトを分けてください（「構造」を別に用意しているのはそのためです）。

---

# 資料を作る（テンプレートができてからの日常）

ここからが本番です。テンプレートは**資料を作るための土台**であって、成果物ではありません。

## 7. 1本の資料ができるまで

```
① データを取る          BigQuery / CSV / 手入力
② 構成を決める          人が決める。「何を言う資料か」
③ 組む                  エージェントがスクリプトで生成（テンプレートを土台にする）
④ PDF にして見る         エージェントが自分で確認 → 人も開いて確認
⑤ 提出用に仕上げる       フォントを入れ直す（embed_fonts.py）
```

③の実例が `scripts/build_organisms.py`（図解を並べたデッキ）と `scripts/build_charts.py`（グラフ見本帳）です。
どちらも **POTX を読み込み、スライドを足して PPTX として書き出す**だけなので、同じ形で自分のレポートを組めます。

**データは1か所に置き、スライド版と文書版で共有します。** 同じ数字を2つのスクリプトに書くと必ずずれます。

## 8. 中身の作法（エージェントへ：書く前に読む）

`bigtree/design/CONTENT_RULES.md` と `CHART_RULES.md` に全部あります。要点：

- **スライドタイトルは「論点」で書き、結論はリード文に置く。**
  ✗「PV は前月比 128%」→ ✓ タイトル「PV とスキの伸び方」＋ リード文「PV は前月比 128%」
- **数値**：本文と表は桁区切り（`12,480`）、グラフの軸とラベルは K / M（`12.5K`）。同じ図で混ぜない
- **強調は1枚に1か所**。色は accent6（自動配色に入っていないので、指定したときだけ出る）
- **図解（図形）とグラフ（データ）を取り違えない。**
  構成比・対比・増減は**データ**なので、図形で描かずネイティブグラフにする。
  図形で描くと、数字が変わるたびに作り直しになります（実際にやり直しました）
- **文字数の上限を超えない。** 折り返しをオフにしてあるので、長い文字は**静かに枠からはみ出します**

```bash
.venv/bin/python scripts/fit_text.py lead "入れたい文章"   # 単体で測る
```

`scripts/fit_text.py` は生成時にも働き、はみ出す文字があれば警告します。
**警告が出たまま提出しない**（文章を削る。文字を小さくしない）。

## 9. グラフ（エージェントへ）

- **横に Excel を書き出し、Excel を正にする**（`data1` / `graph1` …）。
  資料に埋め込まれたグラフはその写し。ずれは `scripts/check_chart_drift.py` で検出できます
- 棒＝足せる量、折れ線＝足せない率。**棒は必ず 0 起点**
- 値ラベルと軸の目盛りは**どちらか一方**だけ
- **ウォーターフォールは chartEx という別形式**（`kind="waterfall-x"`）。
  連結線・増減の色分け・合計指定が、形式の機能として入っています。
  Google スライドに持ち出す資料では、互換版（`kind="waterfall"`、積み上げ棒）を使ってください

## 10. Word で出すとき

スライドと**同じ数字**を使い、作法だけ変えます。

- 結論を最初の1段落に書く（スライドのリード文にあたる）
- 見出し1〜2で章立て → **目次が自動生成される**（開いたとき「フィールド更新」が1回必要）
- 図表は `SEQ` フィールドで**自動採番**。後から図を足しても番号が振り直される
- 本文で説明を書ける分、スライドより情報密度を上げられます。**AI に読ませる資料なら Word のほうが向く**

## 11. 提出直前

```bash
.venv/bin/python scripts/embed_fonts.py 提出用.pptx
```

Word は保存のたびに日本語フォントを「使用文字だけ」に削ります。人が編集し終えたファイルをこれに通します。
**公開・共有用のファイルには埋め込まない**でください（再配布になる／1ファイル 6.6MB＝32倍になる）。

---

## 12. 必ず踏む罠（先に読むと時間が浮く）

> **指定しない＝Office の既定が出る。**

| 罠 | 対処 |
|---|---|
| 表の外枠・縦罫が勝手に出る | 「線なし」も明示する（`<w:val="none">` / `<a:ln><a:noFill/></a:ln>`） |
| グラフの3番目の系列に強調色が付く | 強調色を accent6 に置く（§4） |
| 文字が下にずれる／行間が広い | 行間は pt 指定、内側余白 0。**本文は上揃え**（`notes/TEXT_LAYOUT_NOTES.md`） |
| 文字が枠からはみ出る（折り返しオフ時） | `fit_text.py` で生成時に検出（§8）。`preview.py` でも色で出る |
| Figma で2行に折り返している文字が、Office では1行で溢れる | 図解の中の文字は**折り返しを有効に**する（枠の大きさが決まっているため）。レイアウトのプレースホルダは折り返しオフのまま |
| 濃い塗りの上のラベルが読めない | 塗りの明るさから白／黒を決める（`label_on()`。相対輝度 0.18 が境目） |
| 暗地に変えたら文字が見えない | 色トークンだけでは地色は変わらない。**レイアウト JSON の `bg` と `color` を反転**する。危ない場所は `preview.py` が警告する |
| 自分のロゴ SVG が読めない | 普通の SVG（複数パス・曲線・多色・`transform`）に対応済み。`dxf_to_svg.py` は作者が使っている**任意**の道具で、必須ではない |
| 図形の位置を目分量で書いて重なる | ゾーンの高さから**引き算**して決める |
| Word で挿絵が消える | 「図」スタイルだけ行間を auto に（固定値は行内画像を切り詰める） |
| Word の行間が倍に開く | ドキュメントグリッドを無効化（日本語 Word の既定は行を格子に吸着させる） |
| 「コンテンツに問題が見つかりました」 | マスターのプレースホルダーに種類（`type="body"`）を書く。**修復は押さない**（原因が消える） |
| フォント埋め込みが保存で消えた | **自動操作（AppleScript）での保存では消える**。人が画面から保存すれば残る |
| 新しい形式（chartEx）で開けない | 要素の順番と、色・スタイルの別部品が要る（`notes/OOXML_BASICS.md`） |
| 直したのに一部だけ古いまま | 貼り付け先は **PPTX・XLSX・DOCX の3か所**ある。1つだけ直して満足しない |

## 13. 確認の進め方（エージェントへ）

- **PDF に書き出して自分で見る。** Office に「名前を付けて保存」で PDF を作らせ、画像にして確認する
  （Word のサンドボックスは `/tmp` に書けません。`~/Library/Containers/com.microsoft.Word/Data/Documents/` に出ます）
- **往復テスト**：人が開いて保存し直したファイルを読み、書いた設定が残っているか確かめる。
  スタイルの内部 ID は書き換わるが**名前は残る**ので、**名前で引く**
- **失敗したら、まず「前は動いたファイル」で試す。** アプリが固まっているのか、ファイルが壊れているのかを一手で切り分けられる
- **修復ダイアログが出たら、推測で直さない。** 同じ形式を書き出している OSS のソースと突き合わせるほうが速い。
  検証1回ごとに人の操作が要るので、試行回数そのものがコストです
- **人が開いているファイルを閉じない**（未保存の作業を失う）

## 14. 公開・共有するときの分け方

| 領域 | 中身 | 公開 |
|---|---|---|
| 研究・試作 | 検証ログ、調査メモ、比較用ファイル | しない |
| ブランドの完成品 | テンプレート、トークン、ロゴ | 見本として公開するかは判断 |
| ノウハウ | 手順書・スクリプト・値が空の雛形 | **公開する** |

スクリプトは**ブランドの値を外から受け取る**作り（`OFFICE3_BRAND`）にしておくと、
他の人は自分のトークンとロゴを用意するだけで同じものを作れます。

公開ファイルの作り直しは `scripts/publish_samples.py`。フォントを埋め込まず、
**クライアント名が混ざっていないか中身を検査してから**コピーします（見つかれば止まります）。
仕事のデータを扱うなら、この検査は仕組みとして持っておくことを勧めます。
